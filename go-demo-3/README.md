# Go Demo 3 — Map и структуры данных

## Темы
- Map (создание, чтение, запись, `delete`, итерация)
- Type aliases (`type bookmarkMap = map[string]string`)
- Лейблы циклов (`break Menu`)
- `make` для map

## Итоговый проект
Менеджер закладок — консольное приложение с меню
для добавления, просмотра и удаления закладок (название → URL).